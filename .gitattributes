# Normalize line endings to LF in the repository.
# gofmt, prettier, and the Linux containers this project ships in all expect LF.
* text=auto eol=lf

# Windows scripts need CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binary files.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
